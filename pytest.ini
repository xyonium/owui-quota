[pytest]
# The system python3 has an incompatible starlette (1.3.1) vs the pinned
# fastapi 0.110.0 that the admin module imports. .venv/ holds the pinned,
# self-contained dependency stack (fastapi 0.110.0 + starlette 0.36.3 +
# pydantic 2.13.4 + httpx); prepend it so it shadows the broken pair.
pythonpath = .venv
